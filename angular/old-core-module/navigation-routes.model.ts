export interface NavigationSegments {
    root: string;
    subPaths: any;
}

export interface NavigationRoutesModel {
    firstPath: NavigationSegments;
    secondPath: NavigationSegments;
    thirdPath: NavigationSegments;
}
