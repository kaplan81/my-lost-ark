document.addEventListener('presentationInit', function () {
    var slide = app.slide.&&& = {
        elements: {},
        onEnter: function (ele) {},
        onExit: function (ele) {}
    };
});